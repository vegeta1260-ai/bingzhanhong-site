# 交辦：第五輪影像（筆記專欄四張 + 分享縮圖一張）

## 〇、開工前務必先讀（這段最重要）

**這個 repo 是兩方同時在工作的。**
`.html` / `.css` / `.js` / `.py`、`partials/`、`assets/css/`、`assets/js/` 全部由我維護，
上面隨時有你看不到的未推送修改。**請不要碰這些檔案，也不要重寫 git 歷史。**

前三輪合作，有三次你的合併動作把我已完成的提交蓋掉了，得從 reflog 救回來。
現在 `main` 已經開了分支保護（禁止 force push、禁止刪除分支），所以：

| 絕對不要 | 原因 |
|---|---|
| `git push --force` / `--force-with-lease` | 會被分支保護擋下，而且會覆蓋他人提交 |
| `git rebase main` 之後再合併 | 重寫他人提交雜湊，等同覆蓋 |
| `git reset --hard` 到舊提交再推送 | 這正是前三次事故的原因 |
| 改任何 `.html` `.css` `.js` `.py` | 版面與程式由我維護 |

**正確流程：**

```bash
git checkout main
git pull                          # 開工前一定要先拉
# ...產圖，只新增 assets/img/ 底下的檔案...
git add assets/img
git commit -m "第五輪：筆記專欄用圖"
git push                          # 不要加任何 force 參數
```

完整規則見 repo 根目錄的 `CODEX.md`，開工前請完整讀過一次。

---

## 一、第四輪驗收：三張全過，這個水準請維持

`product-hero.jpg`、`story-hero.jpg`、`story-tainan.jpg` 已合併上線（commit `e0470c3`）。
三張都很好，**而且你這輪沒有動 git，謝謝**。具體對在哪裡，請沿用同一套判斷：

- `story-hero` 是老屋廚房、有人在攪鍋 → **主體是人的動作**
- `story-tainan` 是紅磚老街與鐵窗花 → **主體是空間，完全沒有飲品**
- `product-hero` 是木桌三只玻璃杯配桂花 → **主體是飲品，且左側大片留白給文字面板**

三張並排看，遮住標題也分得出來。這就是 `CODEX.md` 裡「三軸互斥」要的效果。

---

## 二、這輪要做的：筆記專欄四張

這四張會出現在〈手記〉文章的**正文上方**，性質是「說明性」而不是「情境性」：
畫面單純、主體清楚、不需要刻意留白給文字（不會有字壓上去）。

尺寸一律 **960×600（16:10）**，另外輸出同名 `.webp`（品質 80），單張 ≤ 300KB、sRGB。

| 檔名 | 主體 | 景別 | 光線 | 內容 |
|---|---|---|---|---|
| `note-smoke.jpg` | 原料乾貨 | **特寫** | 側逆光 | 烏梅特寫。深褐近黑、表面皺褶與霜紋清楚，側逆光讓紋理跳出來。散放在粗陶盤或竹篩上，背景暗、淺景深 |
| `note-ritual.jpg` | 人的動作 | **中景** | 逆光 | 一隻手把玻璃杯舉向窗光，酸梅湯透光呈紅寶石色。窗框可入鏡，背景虛化。重點是「透光的顏色」 |
| `note-eight.jpg` | 原料乾貨 | **俯拍全景** | 明亮日光 | 八種乾料分別放在八個小碟／小碗中，正上方俯拍，米白或原木桌面，排列整齊留出間距 |
| `note-care.jpg` | 器物與容器 | **中景** | **冰箱冷光** | 打開的冰箱層架，整齊排著幾只**玻璃壺**，旁邊有些日常食材。冷白光，跟其他三張的暖調拉開 |

**四張並排的自我檢查**：景別是 特寫／中景／俯拍／中景，主體是 乾貨／人手／乾貨／器物，
光線是 側逆光／逆光／日光／冷光。**遮住標題還分得出來嗎？分不出來就重做。**

### `note-eight.jpg` 有一個額外限制

這張圖同時用在三個地方，裁切比例不同：

| 位置 | 顯示比例 |
|---|---|
| 手記文章內（`notes.html`） | 16:10 |
| 首頁手記卡片（`index.html`） | 4:3 |
| 故事頁「為什麼是酸梅湯」出血圖（`story.html`） | 4:3 |

所以**八個小碟不要排到貼邊**，四周各留約 8% 的空白，被裁成 4:3 時才不會切掉最外圈的碟子。

---

## 三、另外一張：社群分享縮圖

| 檔名 | 尺寸 | 內容 |
|---|---|---|
| `og-cover.jpg` | **1200×630** | 分享到 LINE／Facebook 時顯示的縮圖。建議用一杯冰鎮酸梅湯搭餐桌，**中央主體、四周留空**——各平台裁切方式不同，邊緣會被吃掉。**不要放任何文字**，標題由平台自己疊 |

這張同樣輸出 `.webp`。

---

## 四、不用你做的

`product-bottle.jpg`（產品瓶身正面照）**請不要生成**。
品牌有實際包裝與標籤，這張必須是實拍照，會由業主提供。
`CODEX.md` 的第一條硬規則（畫面中不得出現產品瓶子）在這張上仍然適用——你不做就對了。

`line-qr.png`、`linepay-qr.png` 也不用做，那是店家後台匯出的檔案。

---

## 五、硬規則複習（違反就不能用）

1. **畫面中不得出現產品瓶子**，需要「裝瓶／冷藏」的畫面請用玻璃壺、玻璃罐、玻璃杯
2. **畫面中不得出現任何文字**，包含招牌、標籤、包裝字樣
3. **不得有療效暗示的視覺**（中藥櫃、藥材舖、藥罐一律不行）
4. 單張 ≤ 300KB、sRGB，同時輸出 `.webp`（品質 80）
5. **檔名要跟上表一字不差**，錯一個字網站就找不到圖
6. 色調：朱紅 `#B51823`、墨 `#161513`、紙白 `#FBF8F1`。要台灣家庭、自然光、手作感；
   不要中國宮廷風、大紅大金、保健食品廣告感、HDR 過曝

## 六、驗收

```bash
python -m http.server 5173
```

開 `http://localhost:5173/notes.html`，確認四篇文章上方的灰色「【待製圖】」框都消失，
390px 寬度下沒有橫向捲動。然後 `git add assets/img && git commit && git push`（不加 force）。
