# 交辦：第八輪——重做兩張原料圖（有實物參照）

## 〇、開工前務必先讀

**這個 repo 是兩方同時在工作的。**
`.html` / `.css` / `.js` / `.py`、`partials/`、`assets/css/`、`assets/js/` 由我維護，
上面隨時有你看不到的未推送修改。**請不要碰這些檔案，也不要重寫 git 歷史。**

| 絕對不要 | 原因 |
|---|---|
| `git push --force` / `--force-with-lease` | 會被分支保護擋下，也會覆蓋他人提交 |
| `git rebase main` 後合併 | 重寫他人提交雜湊，等同覆蓋 |
| `git reset --hard` 到舊提交再推送 | 前三次事故的原因 |
| 改任何 `.html` `.css` `.js` `.py` | 版面與程式由我維護 |

```bash
git checkout main && git pull
# 只新增／替換 assets/img/ 底下的檔案
git add assets/img && git commit -m "第八輪：重做烏梅與梅乾" && git push
```

---

## 一、這輪只有兩張，但要求很具體

業主看了網站，指出 `ing-wumei.jpg`「有點像蜜棗，不夠準確」。**他是對的。**

我把八張原料圖跟業主提供的官方棚拍逐一比對，結果是：

| 檔名 | 判定 |
|---|---|
| `ing-shanzha`（山楂） | ✅ 正確，不要動 |
| `ing-luoshen`（洛神） | ✅ 正確，不要動 |
| `ing-chenpi`（陳皮） | ✅ 正確，不要動 |
| `ing-gancao`（甘草） | ✅ 正確，不要動 |
| `ing-guihua`（桂花） | ✅ 正確，不要動 |
| `ing-bingtang`（冰糖） | ✅ 正確，不要動 |
| **`ing-wumei`（烏梅）** | ❌ **重做** |
| **`ing-meigan`（梅乾）** | ❌ **重做** |

**只做這兩張。其他六張請完全不要碰。**

---

## 二、`ing-wumei.jpg`：烏梅

### 現在錯在哪

目前的圖是：飽滿渾圓、霧面、紅褐色、表面只有淺淺的紋路——
那是**蜜棗或加州蜜棗（prune）**的樣子，不是烏梅。

### 實物長什麼樣（有官方照片可對照）

業主的官方棚拍裡有一木碗真正的烏梅，特徵是：

| 特徵 | 實物 | 目前的圖 |
|---|---|---|
| **顏色** | **近黑**，帶紫黑調，深到幾乎看不出紅 | 紅褐色 |
| **表面** | **有光澤**，像上了一層薄油，會反光 | 霧面 |
| **形狀** | **扁、不規則**，被壓過的感覺 | 飽滿渾圓 |
| **皺褶** | **深而銳利**的摺痕，走向雜亂 | 淺而規則 |
| **大小** | 較小，約一個拇指指節 | 偏大 |

烏梅是青梅**煙燻乾燥**後的樣子，所以它是縮、扁、黑、亮的，
不是曬乾後那種蓬鬆飽滿的果乾。

**參考圖我另外附上**（從官方棚拍裁出來的真實烏梅），請照著做。

### 規格

- **600×600**，正方形
- 構圖與其餘六張**完全一致**：正上方俯拍、米白色微粒質感背景、
  食材堆成一小堆置中、柔和均勻棚燈、淡而短的陰影
- 這張會跟另外七張並排出現，**背景色與光線必須對得上**

---

## 三、`ing-meigan.jpg`：梅乾

### 現在錯在哪

目前的圖看起來像**葡萄乾**：小、橢圓、表面平滑、紅棕色。

### 要改成

梅乾是**醃漬後乾燥的梅子**：

- 顏色：**深褐到黑褐**，比烏梅淺一點但仍偏深
- 表面：**佈滿細密皺褶**，可能帶一層薄薄的白霜（鹽霜）
- 形狀：略扁的圓形，比葡萄乾**大上兩三倍**，看得出是梅子而不是莓果
- 有些可以露出一點果核的輪廓

**關鍵是要跟 `ing-wumei` 分得出來**：烏梅近黑帶光澤，梅乾深褐帶白霜、更乾癟。
兩張並排時如果分不出來，就是還沒到位。

### 規格

同上：**600×600**，構圖、背景、光線與其餘六張一致。

---

## 四、硬規則

1. 單張 **≤ 300KB**、sRGB，同時輸出 **`.webp`**（品質 80）
2. **檔名一字不差**：`ing-wumei.jpg` / `ing-wumei.webp` / `ing-meigan.jpg` / `ing-meigan.webp`
3. 畫面中**不得出現任何文字**
4. **不要生成品牌瓶子**
5. 不得有療效暗示的視覺（中藥櫃、藥材舖、藥罐）
6. 色調對齊官方棚拍：米白 `#F5F2ED` 背景、暖褐 `#573727`

## 五、驗收

```bash
python -m http.server 5173
```

開 `http://localhost:5173/product.html`，捲到「八種食材，各司其職」：

- 八張並排，**背景色與光線看不出哪兩張是新做的**
- **烏梅要看起來像烏梅，不是蜜棗**
- **烏梅與梅乾要分得出來**

完成後 `git add assets/img && git commit && git push`（不加 force）。
