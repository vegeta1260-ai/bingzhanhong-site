# 交辦：第六輪影像（四種喝法重做 + 一張製程替換）

## 〇、開工前務必先讀

**這個 repo 是兩方同時在工作的。**
`.html` / `.css` / `.js` / `.py`、`partials/`、`assets/css/`、`assets/js/` 全部由我維護，
上面隨時有你看不到的未推送修改。**請不要碰這些檔案，也不要重寫 git 歷史。**

| 絕對不要 | 原因 |
|---|---|
| `git push --force` / `--force-with-lease` | 會被分支保護擋下，而且會覆蓋他人提交 |
| `git rebase main` 之後再合併 | 重寫他人提交雜湊，等同覆蓋 |
| `git reset --hard` 到舊提交再推送 | 這是前三次事故的原因 |
| 改任何 `.html` `.css` `.js` `.py` | 版面與程式由我維護 |

```bash
git checkout main
git pull                          # 開工前一定要先拉
# ...產圖，只新增／替換 assets/img/ 底下的檔案...
git add assets/img
git commit -m "第六輪：四種喝法重做"
git push                          # 不要加任何 force 參數
```

第四、五輪你都遵守了，很好，請繼續。完整規則見 `CODEX.md`。

---

## 一、這輪的原因：drink-1～4 並排看是同一張圖

`drink-1.jpg` 到 `drink-4.jpg` 是第一輪產的，那時「三軸互斥」規則還沒寫。
現在四張在產品頁**並排出現**，實際效果是：

> 同一只玻璃杯、同一張木桌、同一扇窗、同一盆植物，
> 只有液體深淺不同，其中一張多了一盤菜。

遮住標題完全分不出來。這四張要**全部重做**。

---

## 二、四種喝法：四張的三軸必須互斥

尺寸一律 **800×1000（直式 4:5）**，另出同名 `.webp`（品質 80），單張 ≤ 300KB、sRGB。
**直式**很重要——版面已經改成 4:5，橫圖會被裁掉兩側。

| 檔名 | 標題 | 景別 | 主體 | 光線 | 內容 |
|---|---|---|---|---|---|
| `drink-1.jpg` | 冰鎮純飲 | **特寫**（杯子占畫面 70%） | 飲品本身 | 柔和側光、暗背景 | 一只玻璃杯的深紅酸梅湯，**不加冰塊**。杯壁凝著水珠，背景壓暗讓紅色跳出來，淺景深。看不到桌面環境 |
| `drink-2.jpg` | 加冰塊 | **正上方俯拍** | 器物與容器 | 明亮日光 | 從杯口正上方往下拍，看到冰塊浮在液面、杯緣、桌面紋理。白色或淺色桌面，構圖乾淨對稱 |
| `drink-3.jpg` | 兌氣泡水 | **中景** | 人的動作 | 逆光 | 一隻手正把氣泡水倒進杯裡，液面顏色比其他張淺，氣泡明顯上升。逆光讓氣泡發亮，背景是窗 |
| `drink-4.jpg` | 餐後解膩 | **全景** | 食物與餐桌 | 夜晚暖燈 | 吃完的餐桌：盤子裡剩一些菜、筷子擱著、椅子入鏡。杯子在畫面一角，不是主角。暖黃燈光，和前三張的白天調性完全拉開 |

### 交稿前的自我檢查

把四張並排，**遮住標題**：

- 景別分得出來嗎？（特寫／俯拍／中景／全景）
- 光線分得出來嗎？（側光／日光／逆光／夜燈）
- 有沒有兩張用同一個背景？（**這是上一版失敗的主因**）

任何一項答不出來就重做。

### 特別提醒

四張**不要用同一組場景微調**。請當成四個不同時間、不同房間、不同心情拍的四張照片。
第 4 張尤其要離得遠——那是晚上、吃完飯、燈是暖的。

---

## 三、順便替換一張製程照

| 檔名 | 現況 | 要改成 |
|---|---|---|
| `process-cool.jpg` | 和 `process-pot.jpg` 太像，都是「木檯面上的一只鍋」 | 改成**特寫**：熬好的深紅湯汁正從鍋裡倒進玻璃容器，蒸氣與液流清楚，主體占畫面 70%。跟 `process-pot`（中景、爐上冒煙的鍋）明確區隔 |

尺寸沿用 **800×600**，其餘規格同上。

`process-prep`、`process-pot`、`process-chill` 三張維持不動，不用重做。

---

## 四、硬規則複習（違反就不能用）

1. **畫面中不得出現產品瓶子。** 要表現裝瓶／冷藏請用玻璃壺、玻璃罐、玻璃杯
2. **畫面中不得出現任何文字**，包含招牌、標籤、包裝字樣
3. **不得有療效暗示的視覺**（中藥櫃、藥材舖、藥罐一律不行）
4. 單張 ≤ 300KB、sRGB，同時輸出 `.webp`（品質 80）
5. **檔名要一字不差**，錯一個字網站就找不到圖
6. 色調：朱紅 `#B51823`、墨 `#161513`、紙白 `#FBF8F1`。要台灣家庭、自然光、手作感；
   不要中國宮廷風、大紅大金、保健食品廣告感、HDR 過曝

## 五、不用你做的

`product-bottle.jpg`（瓶身正面照）**請不要生成**——品牌有實際包裝，必須實拍，由業主提供。
`line-qr.png`、`linepay-qr.png` 是店家後台匯出的檔案，也不用做。

## 六、驗收

```bash
python -m http.server 5173
```

開 `http://localhost:5173/product.html`，捲到「四種喝法」：

- 四張並排，**遮住標題仍分得出是四張不同的照片**
- 四張等高、沒有變形（版面是 4:5，你的圖也要是 4:5）
- 390px 寬度下沒有橫向捲動

完成後 `git add assets/img && git commit && git push`（不加 force），
並在描述裡說明每張圖的產出方式與檔案大小。
