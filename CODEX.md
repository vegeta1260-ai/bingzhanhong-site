# 給 Codex 的工作規範

這份是每次交辦都適用的規則。**開工前請完整讀過**。

---

## 一、Git 規則（最重要，請務必遵守）

這個 repo 由兩方同時工作：**Claude 負責網站程式與版面，你負責影像素材**。
過去三次協作，你的合併動作覆蓋掉了對方已完成的提交，必須避免。

### 一定要做

```bash
git checkout main
git pull                      # 開工前務必先拉，確保基於最新版
```

### 絕對不要做

| 禁止 | 原因 |
|---|---|
| `git push --force` / `--force-with-lease` | `main` 已設分支保護，會被拒絕；也會覆蓋他人提交 |
| `git rebase main` 後再合併 | 會重寫他人提交的雜湊，等同覆蓋 |
| `git reset --hard` 到舊提交後再推送 | 這正是前三次事故的原因 |
| 修改 `.html`、`.css`、`.js`、`.py` 任何檔案 | 版面與程式由 Claude 維護，你動了會衝突 |
| 修改 `partials/`、`assets/css/`、`assets/js/` | 同上 |

### 你只需要動這些

- `assets/img/` 底下的圖片檔（新增或替換）
- 若要記錄產出方式，可在 PR 描述裡寫，不要改 README

### 合併方式

開分支請用 `codex/xxx`，完成後**開 PR，用 merge（不要 squash、不要 rebase）**。
或者更簡單：直接在 `main` 上 commit 圖片後 `git push`（不加任何 force 參數）。

---

## 二、影像規範

### 硬規則（違反就不能用）

1. **不要生成品牌瓶子。** 實品是紅色霧面塑膠瓶配直式紙標籤，標籤上有中文與圓形徽章，
   生成的版本一定對不上。需要瓶子的畫面一律用官方棚拍，由 Claude 接。
   要表現「一份酸梅湯」時，**優先用白瓷小盞**（品牌叫「冰盞紅」，盞是官方照片裡的實際器物），
   其次是青花瓷碗；玻璃壺、玻璃杯只在需要看見冰塊或氣泡時使用。
2. **畫面中不得出現任何文字**，包含招牌、標籤、包裝字樣。
3. **不得出現任何療效或宣稱的視覺暗示**（例如藥材鋪、中藥櫃、藥罐）。
4. 每張 **≤ 300KB**、**sRGB**，並同時輸出 `.webp`（品質 80）。
5. 檔名與規格書**完全一致**，錯一個字網站就找不到。

### 最重要的一條：同一區塊裡的圖不能長得像

網站上圖片是**並排出現**的。四張並排時如果都是「桌上一杯飲料」，
就算場景不同，視覺上也是同一張圖換四個標題。

**每一組並排的圖，必須在下面三個維度上互斥：**

| 維度 | 選項（同組內不可重複） |
|---|---|
| **景別** | 特寫（主體占畫面 70%）／中景（看得到桌面環境）／全景（看得到房間或戶外）／俯拍 |
| **主體** | 飲品本身／食物與餐桌／人的動作（手）／器物與容器／原料乾貨／空間場景 |
| **光線** | 明亮日光／柔和側光／逆光／夜晚暖燈／冰箱冷光 |

交稿前請自己並排檢查一次：**如果把標題遮起來，還分得出這是四張不同的照片嗎？**
分不出來就重做。

### 色調

品牌色（取自官方棚拍量測值）：朱紅 `#B41016`、灰藍水泥 `#CDD4E0`、
紙白 `#F5F2ED`、暖褐木頭 `#573727`、墨 `#1A1512`。
要的感覺：台灣家庭、自然光、手作感、乾淨留白。
不要：中國宮廷風、大紅大金、藥材舖、保健食品廣告、濃重濾鏡、HDR 過曝。

### 構圖

會被文字壓在上面或滿版出血的圖（規格書會註明），
**主體偏一側、另一側留乾淨空間，下方三分之一亮度不要太高**（要放白字）。

---

## 三、驗收

```bash
python -m http.server 5173
```

開 `http://localhost:5173`，確認：

- 指定位置的「【待製圖】」灰框都消失
- 390px 寬度下圖片正常、沒有橫向捲動
- 四格並排時，遮住標題仍分得出是不同照片

完成後開 PR 或直接 push（不加 force），並在描述裡說明每張圖的產出方式與檔案大小。
