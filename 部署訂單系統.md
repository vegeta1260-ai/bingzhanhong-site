# 開通訂單系統（約 10 分鐘）

做完這五步，網站的**訂單送出**、**付款回報**、**訂單查詢**、**團購詢價**就會開始運作。
目前這四個功能的按鈕是**刻意停用**的——沒有後端卻讓它假裝送出成功，比停用更糟。

**全程只需要一個 Google 帳號。不用寫程式、不用付費。**

最後你只要把一個網址傳給我，其餘我來接。

---

## 第 1 步　建立試算表

1. 開 https://sheets.google.com
2. 點「**空白試算表**」
3. 左上角檔名改成 **`冰盞紅訂單`**（名字隨你，方便日後找到就好）

先不要動裡面的格子，工作表會自動建立。

---

## 第 2 步　貼上程式

**這一步的順序很重要，不能跳過。**

1. 在剛才那份試算表裡，點上方選單 **「擴充功能」→「Apps Script」**
2. 會開一個新分頁，裡面有一個 `Code.gs`，內容大概是：
   ```
   function myFunction() {
   }
   ```
3. **把那幾行全部刪掉**（Ctrl+A 再按 Delete）
4. 打開我附給你的 **`Code.gs`**，全選複製，貼進去
5. 按 **Ctrl+S** 存檔

> ### ⚠️ 一定要從試算表裡面開 Apps Script
> 不能直接去 script.google.com 開一個新的。
> 程式裡用到「**目前這份試算表**」，只有從試算表開的指令碼才找得到它。
> 如果你是從 script.google.com 開的，執行時會出錯，請刪掉重來。

### 想收訂單通知信的話（選用）

程式第 12 行有一行：

```js
var NOTIFY_EMAIL = '';
```

把你的信箱填進去，例如：

```js
var NOTIFY_EMAIL = 'luoxinfi25@gmail.com';
```

填了之後，每筆新訂單與每筆付款回報都會寄一封信給你。不填就不寄。

---

## 第 3 步　執行一次 setup

1. 編輯器上方有個下拉選單，預設可能是 `doPost`。**把它改選成 `setup`**
2. 按旁邊的 **「執行」**
3. 第一次會跳出授權視窗，依序點：
   - **檢閱權限**
   - 選你的 Google 帳號
   - 出現「**Google 尚未驗證這個應用程式**」→ 點左下角小字 **「進階」** → **「前往 冰盞紅訂單（不安全）」**
   - **允許**

> 「不安全」那句話是因為這是你自己寫的程式、沒有經過 Google 審核。
> 你是作者也是唯一使用者，這裡按下去是正常的。

4. 回到試算表分頁，應該會看到自動長出四個工作表：
   **總覽**、**訂單**、**付款回報**、**團購詢價**

看到這四個，第 3 步就成功了。

---

## 第 4 步　部署成網頁

1. 回到 Apps Script 編輯器，右上角點 **「部署」→「新增部署作業」**
2. 左邊齒輪圖示 **「選取類型」→ 選「網頁應用程式」**
3. 三個欄位這樣填：

| 欄位 | 選什麼 | 為什麼 |
|---|---|---|
| 說明 | `冰盞紅訂單 v1`（隨意） | 只是給自己看的備註 |
| **執行身分** | **我（你的信箱）** | 讓程式用你的權限寫入你的試算表 |
| **誰可以存取** | **所有人** | 網站訪客送出訂單時是匿名的，選其他選項會全部被擋下 |

4. 按 **「部署」**
5. 出現一個 **網頁應用程式網址**，長這樣：

```
https://script.google.com/macros/s/AKfycb................/exec
```

6. 按旁邊的 **「複製」**

> ### ⚠️ 要結尾是 `/exec` 的那一個
> 如果你複製到結尾是 **`/dev`** 的網址，那是測試用的，只有你自己登入時能用，
> 客人會全部失敗。**認明 `/exec`。**

---

## 第 5 步　把網址傳給我

複製好之後直接貼給我就行，我會：

- 填進網站設定
- 實際送一筆測試訂單，確認有寫進你的試算表
- 確認訂單查詢、付款回報、團購詢價都通
- 把測試資料刪掉

---

## 你自己先驗一次（選用）

想先確認網址是對的，可以跑這行，把 `你的網址` 換掉：

```bash
curl -s -X POST -H "Content-Type: text/plain" -d "{\"action\":\"lookup\",\"orderNo\":\"TEST\",\"phone\":\"0900000000\"}" "你的網址"
```

**正確**會回：

```json
{"ok":true,"order":null}
```

（查無此單是對的，因為還沒有任何訂單）

**如果回的是一大串 HTML**，表示「誰可以存取」沒有選「所有人」，回第 4 步改。

---

## 日後維護

| 情況 | 要做什麼 |
|---|---|
| **我改了程式** | 你要重新部署：「部署」→「**管理部署作業**」→ 鉛筆圖示 → 版本選「**新版本**」→ 部署。**網址不會變**，不用再傳給我 |
| 想換通知信箱 | 改第 12 行的 `NOTIFY_EMAIL`，存檔，然後照上面重新部署 |
| 想停用訂單系統 | 「管理部署作業」→ 停用即可，網站會自動退回目前的停用狀態 |

---

## 一件要知道的事

「誰可以存取：**所有人**」是必要的——客人下單時沒有登入你的 Google 帳號。

這代表**知道這個網址的人都能往你的試算表寫資料**。程式裡有防機器人的隱藏欄位，
但擋不住有心人。實務上小型網站這樣做很常見，風險不高；
萬一真的被灌垃圾資料，跟我說一聲，刪掉部署重新產生一個新網址就好，
**舊網址會立刻失效**。
